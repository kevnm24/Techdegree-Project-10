import React from 'react';

// this will be used as a message when the user doesn't have acces to the page

const Forbidden = () => {
    <div className="bounds">
      <h1>Forbidden</h1>
      <p> Oh oh! You can't access this page.</p>
    </div>
};

export default Forbidden;
